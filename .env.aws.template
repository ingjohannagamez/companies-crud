# AWS Deployment Environment Variables Template
# Copy this file and set the actual values for your AWS environment

# Database Configuration (AWS RDS)
DATABASE_URL=jdbc:postgresql://your-rds-endpoint.region.rds.amazonaws.com:5432/companies
DATABASE_USERNAME=your_db_username
DATABASE_PASSWORD=your_db_password

# Application Port (typically 8080 for Elastic Beanstalk)
PORT=8080

# File Upload Path (use S3 bucket in production)
UPLOAD_PATH=s3://your-bucket-name/uploads/

# Spring Profile
SPRING_PROFILES_ACTIVE=aws

# Optional: AWS Specific Settings
AWS_REGION=us-east-1
