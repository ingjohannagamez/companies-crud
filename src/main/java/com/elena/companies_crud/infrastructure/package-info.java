/**
 * Agrupa los <b>adaptadores de infraestructura</b> de la aplicación.
 *
 * <p>Las clases de este paquete conectan el dominio con tecnologías
 * concretas como bases de datos, mensajería o configuración externa.</p>
 *
 * <p>Se organiza en subpaquetes como:</p>
 * <ul>
 *     <li>{@code repository} – adaptadores de persistencia.</li>
 *     <li>{@code config} – configuración técnica (por ejemplo, OpenAPI).</li>
 * </ul>
 */
package com.elena.companies_crud.infrastructure;