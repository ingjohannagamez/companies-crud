/**
 * Contiene el <b>modelo de dominio</b> de la aplicación.
 *
 * <p>Las clases de este paquete representan entidades y agregados,
 * como {@code Company}, que encapsulan datos y reglas propias del negocio.</p>
 *
 * <p>Aunque las entidades puedan estar anotadas con JPA para persistencia,
 * su diseño se centra en el dominio y no en la infraestructura.</p>
 */
package com.elena.companies_crud.domain.model;